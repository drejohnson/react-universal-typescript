query AllEpisodes {
  allEpisodes {
    ...EpisodeEntry
    show {
      ...ShowEntry
    }
  }
}

query Episode($id: ID) {
  Episode (id: $id) {
    ...EpisodeEntry
    show {
      ...ShowEntry
    }
  }
}